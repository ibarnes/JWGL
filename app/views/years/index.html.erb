<h1>Listing years</h1>

<table>
  <tr>
    <th>Year</th>
    <th>Start date</th>
    <th>End date</th>
  </tr>

<% @years.each do |year| %>
  <tr>
    <td><%=h year.year %></td>
    <td><%=h year.start_date %></td>
    <td><%=h year.end_date %></td>
    <td><%= link_to 'Show', year %></td>
    <td><%= link_to 'Edit', edit_year_path(year) %></td>
    <td><%= link_to 'Destroy', year, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New year', new_year_path %>